# Database
neshama.db
neshama.db-shm
neshama.db-wal
neshama*.db.bak*
neshama*.db-wal
neshama*.db-shm

# iCloud conflict copies — numbered duplicates like "file 2.html", "file 3.py"
# Never commit. Review on disk first (may contain MSI work) before deleting.
* 2.*
* 3.*
* 4.*
.backup */
.backup\ *

# iCloud offloaded placeholders (download stubs, not content)
.*.icloud
*.icloud

# Desktop temp / old bundles (not code)
NESHAMA-ONE-PAGER.pdf
deploy-v3.sh

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.env.*
*.env

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node (from Puppeteer testing)
node_modules/
package-lock.json
package.json

# Logs
*.log
fonts/
neshama-code.tar.gz

# Claude Code config (contains auth tokens)
.mcp.json
.claude/

# ── Data dumps, exports, and binaries that must never be committed (added 2026-06-02) ──
# Database backups / dumps (the backups/ dir holds 18MB+ prod JSON dumps)
backups/
*.db
*.sqlite
*.sqlite3
backup.json

# Apple Numbers spreadsheets + the photo-audit CSV export (working data, not deliverables)
*.numbers
Neshama_Vendor_Photo_Audit/

# Zip archives + the GSC coverage export (folder and zip)
*.zip
https___neshama.ca_-Coverage-*/

# Stray git worktrees accidentally created inside the repo root (prune these, never commit)
/neshama-caterer-count/
/neshama-demo-seed/

# Stray preview render at repo root
/preview.webp

# Kept on disk, deliberately not committed (Erin's call)
/gsc-indexing-audit-2026-05-14.md
marketing-kit/general/archive/
